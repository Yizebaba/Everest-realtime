# 四大引擎独立查重与协同判重实验控制总表 (Deduplication Matrix Manual)

本文档专门用于配置 **去重（查重）机制** 的实验。
**绝对不改动当前正在运行的后台代码，不上传 GitHub**。
四大引擎将同步对每一条进来的数据进行“是不是重复信息、该不该拦截”的独立判定打擂台！

---

## 一、四大引擎在“查重”上的绝技与分工

1. **LightCEP 查重**：**核心业务指纹查重**
   - 不看全网页花哨的边角料，只盯新闻标题列表或核心数据字段；
   - 支持字符串哈希与正文标题相似度去重。
2. **OpenCEP 查重**：**时序生命周期查重（Temporal Lifecycle）**
   - 关注事件在时间轴上的“发展状态”；
   - 同一个灾害事件在持续发酵期间，如果数据没有进一步恶化，自动判定为时序冗余并压制。
3. **Siddhi 查重**：**流式滑动窗口与状态去重（Window Unique & Aggregation）**
   - 类似大数据流处理中的 `unique:first()` 算子；
   - 规定在设定好的时间窗口（如 30 分钟）内，同一主题或测站只放行第 1 次，其余全部作为重复流过滤。
4. **Sigma 查重**：**特征签名防碰撞查重（Entity & Signature Match）**
   - 提取地震的（经纬度坐标 + 震级）或者新闻的核心实体（人物/地点/灾种）；
   - 若数据库中已存在高度相似的实体签名，则打上“已知重复特征”标签。

---

## 二、您可以自由填写的查重选项（请在 [ ] 中打 X 或修改数字）

---

### 【第一部分：新闻与文本类数据源查重维度】

#### 选项 1.1（去重颗粒度选择）：
- [ ] **A. 全页面字符串哈希**（只要网页多了几个字就算新内容，容易被侧边栏动态广告误导）
- [X] **B. 核心新闻标题集合去重 (推荐)**（只看页面上最新 5 篇新闻的标题，标题没变绝不触发）
- [✅️ ] **C. 独立文章 URL 绝对去重**（只要该文章网址被推送过一次，永远视为重复内容）

#### 选项 1.2（语义与标题相似度阈值）：
有时候不同媒体报道同一件事，标题字句稍有不同。
- 标题相似度达到多少算同一件事：>= [ 70 ] % 则视为重复并拦截

#### 选项 1.3（负向噪音自动压制）：
- [X] 开启：若本次内容相比上次仅仅是更新了“换届选举、商业赞助、历史回顾、纳税报告”，直接判定为无效重复，强制静默。

---

### 【第二部分：地震数值流查重维度】

#### 选项 2.1（官方唯一编号查重 - Event ID）：
- [X] 开启：必须出现全新的官方事件 ID（如 USGS 的 `us7000ti15`，中国地震台网的 `CC.xxxx`），否则 100% 判定为重复记录。

#### 选项 2.2（时空邻近地震防抖合并 - Spatial-Temporal Clustered）：
有时候同一场大地震，各家台网几分钟内会连续微调经纬度和震级。
- 震中距离误差在：<= [ 1.0 ] km 内
- **且**发震时间差在：<= [ 180 ] 分钟内
- 是否自动合并为同一场地震（不重复发信）：[ 是 ] (填 是 或 否)

---

### 【第三部分：气象多维数值防抖查重维度】

#### 选项 3.1（微小数值扰动死区 - Deadband Filter）：
高原风速和气温每秒都在微跳，微小的变化不能算作有新情况。
- 峰顶阵风波动不超过：<= [ 3.0 ] km/h 视为重复无变化，忽略不报
- 峰顶气温波动不超过：<= [ 1.0 ] ℃ 视为重复无变化，忽略不报
- 地表气压波动不超过：<= [ 1.5 ] hPa 视为重复无变化，忽略不报

#### 选项 3.2（恶化加剧才重报机制 - Escalation Trigger）：
- [X] 开启：在同一天内，如果已经推过一次大风暴，只有当新风速比上一次**大出 >= [ 10.0 ] km/h** 时，才允许再次作为新险情推送！

---

### 【第四部分：时间窗口与时效抑制（Siddhi & OpenCEP 窗口）】

#### 选项 4.1（持续事件静默抑制期 - Cooldown Period）：
同一个灾害或异常状态持续发生时，防止微信每 3 分钟被轰炸一次。
- 单个数据源推送后的强制冷却保护期：[ 30 ] 分钟（在这 30 分钟内哪怕一直符合条件，也静默不打扰）

#### 选项 4.2（时序状态演进追踪 - OpenCEP Lifecycle）：
- [对 ] 开启：只在灾害状态发生“量级跃迁”（比如：从 黄色预警 ➔ 升级为 红色特急警报）时，才解除去重拦截进行追加推送。

---

### 【第五部分：四大引擎查重打擂与决策表决】

请选择当数据到来时，四大引擎如何决定它算不算“重复骚扰”：
- [X] **模式 A：独立查重打擂观察**：4 个引擎都在后台打分，微信推送时在底部附带：
  `【查重判定】LightCEP:[新数据] | OpenCEP:[新数据] | Siddhi:[冷却中] | Sigma:[已知事件]`
- [ ] **模式 B：一票否决制（极度安静）**：只要有任意 1 个引擎判定这是“重复或噪音”，立即取消发送！
- [ ] **模式 C：共识去重制（平衡模式）**：必须至少 2 个引擎同时判定这是“全新灾害”，才放行发送。

---

## 三、填写与保存说明
您可以在上面的 `[ ]` 中自由打勾 `[X]`，或者直接修改中括号里的数字。
修改完后按键盘 **`Ctrl + S`** 保存文件即可！
